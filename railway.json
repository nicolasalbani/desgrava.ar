{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "dockerfilePath": "Dockerfile"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && next start --port ${PORT:-3000}",
    "healthcheckPath": "/api/auth/session",
    "restartPolicyType": "ON_FAILURE"
  }
}
